// NOTICE:
// This file is generated by corresponding ".tt" file template, do NOT modify this file.

using System.Runtime.CompilerServices;

using Althea.Backend.Cuda.Storage;
using Althea.LinearAlgebra;


namespace Althea.Backend.Cuda;

/// <summary>
/// The struct with nothing for GPU 0.
/// </summary>
public readonly struct GpuId0 : IGpuId
{
	static short IGpuId.GpuId
	{
		[MethodImpl(MethodImplOptions.AggressiveInlining)]
		get => 0;
	}
}

/// <summary>
/// The struct with nothing for GPU 1.
/// </summary>
public readonly struct GpuId1 : IGpuId
{
	static short IGpuId.GpuId
	{
		[MethodImpl(MethodImplOptions.AggressiveInlining)]
		get => 1;
	}
}

/// <summary>
/// The struct with nothing for GPU 2.
/// </summary>
public readonly struct GpuId2 : IGpuId
{
	static short IGpuId.GpuId
	{
		[MethodImpl(MethodImplOptions.AggressiveInlining)]
		get => 2;
	}
}

/// <summary>
/// The struct with nothing for GPU 3.
/// </summary>
public readonly struct GpuId3 : IGpuId
{
	static short IGpuId.GpuId
	{
		[MethodImpl(MethodImplOptions.AggressiveInlining)]
		get => 3;
	}
}

/// <summary>
/// The struct with nothing for GPU 4.
/// </summary>
public readonly struct GpuId4 : IGpuId
{
	static short IGpuId.GpuId
	{
		[MethodImpl(MethodImplOptions.AggressiveInlining)]
		get => 4;
	}
}

/// <summary>
/// The struct with nothing for GPU 5.
/// </summary>
public readonly struct GpuId5 : IGpuId
{
	static short IGpuId.GpuId
	{
		[MethodImpl(MethodImplOptions.AggressiveInlining)]
		get => 5;
	}
}

/// <summary>
/// The struct with nothing for GPU 6.
/// </summary>
public readonly struct GpuId6 : IGpuId
{
	static short IGpuId.GpuId
	{
		[MethodImpl(MethodImplOptions.AggressiveInlining)]
		get => 6;
	}
}

/// <summary>
/// The struct with nothing for GPU 7.
/// </summary>
public readonly struct GpuId7 : IGpuId
{
	static short IGpuId.GpuId
	{
		[MethodImpl(MethodImplOptions.AggressiveInlining)]
		get => 7;
	}
}

/// <summary>
/// The struct with nothing for GPU 8.
/// </summary>
public readonly struct GpuId8 : IGpuId
{
	static short IGpuId.GpuId
	{
		[MethodImpl(MethodImplOptions.AggressiveInlining)]
		get => 8;
	}
}

/// <summary>
/// The struct with nothing for GPU 9.
/// </summary>
public readonly struct GpuId9 : IGpuId
{
	static short IGpuId.GpuId
	{
		[MethodImpl(MethodImplOptions.AggressiveInlining)]
		get => 9;
	}
}

/// <summary>
/// The struct with nothing for GPU 10.
/// </summary>
public readonly struct GpuId10 : IGpuId
{
	static short IGpuId.GpuId
	{
		[MethodImpl(MethodImplOptions.AggressiveInlining)]
		get => 10;
	}
}

/// <summary>
/// The struct with nothing for GPU 11.
/// </summary>
public readonly struct GpuId11 : IGpuId
{
	static short IGpuId.GpuId
	{
		[MethodImpl(MethodImplOptions.AggressiveInlining)]
		get => 11;
	}
}

/// <summary>
/// The struct with nothing for GPU 12.
/// </summary>
public readonly struct GpuId12 : IGpuId
{
	static short IGpuId.GpuId
	{
		[MethodImpl(MethodImplOptions.AggressiveInlining)]
		get => 12;
	}
}

/// <summary>
/// The struct with nothing for GPU 13.
/// </summary>
public readonly struct GpuId13 : IGpuId
{
	static short IGpuId.GpuId
	{
		[MethodImpl(MethodImplOptions.AggressiveInlining)]
		get => 13;
	}
}

/// <summary>
/// The struct with nothing for GPU 14.
/// </summary>
public readonly struct GpuId14 : IGpuId
{
	static short IGpuId.GpuId
	{
		[MethodImpl(MethodImplOptions.AggressiveInlining)]
		get => 14;
	}
}

/// <summary>
/// The struct with nothing for GPU 15.
/// </summary>
public readonly struct GpuId15 : IGpuId
{
	static short IGpuId.GpuId
	{
		[MethodImpl(MethodImplOptions.AggressiveInlining)]
		get => 15;
	}
}

internal static class CudaMemoryPointerExtension
{
	[MethodImpl(MethodImplOptions.AggressiveInlining)]
	internal static bool FromGenericGpu<TP>(this TP pointer, out CudaMemoryPointer ptr) where TP : IPointer<TP>
	{
		ptr = default;
		if (pointer is CudaMemoryPointer<GpuId0>)
		{
			if (Runtime.CurrentDeviceID != 0)
				return false;
			goto END;
		}
		if (pointer is CudaMemoryPointer<GpuId1>)
		{
			if (Runtime.CurrentDeviceID != 1)
				return false;
			goto END;
		}
		if (pointer is CudaMemoryPointer<GpuId2>)
		{
			if (Runtime.CurrentDeviceID != 2)
				return false;
			goto END;
		}
		if (pointer is CudaMemoryPointer<GpuId3>)
		{
			if (Runtime.CurrentDeviceID != 3)
				return false;
			goto END;
		}
		if (pointer is CudaMemoryPointer<GpuId4>)
		{
			if (Runtime.CurrentDeviceID != 4)
				return false;
			goto END;
		}
		if (pointer is CudaMemoryPointer<GpuId5>)
		{
			if (Runtime.CurrentDeviceID != 5)
				return false;
			goto END;
		}
		if (pointer is CudaMemoryPointer<GpuId6>)
		{
			if (Runtime.CurrentDeviceID != 6)
				return false;
			goto END;
		}
		if (pointer is CudaMemoryPointer<GpuId7>)
		{
			if (Runtime.CurrentDeviceID != 7)
				return false;
			goto END;
		}
		if (pointer is CudaMemoryPointer<GpuId8>)
		{
			if (Runtime.CurrentDeviceID != 8)
				return false;
			goto END;
		}
		if (pointer is CudaMemoryPointer<GpuId9>)
		{
			if (Runtime.CurrentDeviceID != 9)
				return false;
			goto END;
		}
		if (pointer is CudaMemoryPointer<GpuId10>)
		{
			if (Runtime.CurrentDeviceID != 10)
				return false;
			goto END;
		}
		if (pointer is CudaMemoryPointer<GpuId11>)
		{
			if (Runtime.CurrentDeviceID != 11)
				return false;
			goto END;
		}
		if (pointer is CudaMemoryPointer<GpuId12>)
		{
			if (Runtime.CurrentDeviceID != 12)
				return false;
			goto END;
		}
		if (pointer is CudaMemoryPointer<GpuId13>)
		{
			if (Runtime.CurrentDeviceID != 13)
				return false;
			goto END;
		}
		if (pointer is CudaMemoryPointer<GpuId14>)
		{
			if (Runtime.CurrentDeviceID != 14)
				return false;
			goto END;
		}
		if (pointer is CudaMemoryPointer<GpuId15>)
		{
			if (Runtime.CurrentDeviceID != 15)
				return false;
			goto END;
		}
		return false;
	END:
		ptr = Unsafe.As<TP, CudaMemoryPointer>(ref pointer);
		return true;
	}

	[MethodImpl(MethodImplOptions.AggressiveInlining)]
	internal static bool FromGenericFile<TP>(this TP pointer, out CudaFilePointer ptr) where TP : IPointer<TP>
	{
		if (pointer is CudaFilePointer p)
		{
			ptr = p;
			return true;
		}
		else
		{
			ptr = default;
			return false;
		}
	}

	[MethodImpl(MethodImplOptions.AggressiveInlining)]
	internal static (CudaMemoryPointer, CudaFilePointer) FromGeneric<TP>(this TP pointer) where TP : IPointer<TP>
	{
		(CudaMemoryPointer, CudaFilePointer) result = default;
		if (FromGenericFile(pointer, out var f))
		{
			result.Item2 = f;
		}
		if (FromGenericGpu(pointer, out var g))
		{
			result.Item1 = g;
		}
		return result;
	}
}

internal static unsafe partial class MemoryPointerChecker
{
	#region CUDA back-end
	[MethodImpl(MethodImplOptions.AggressiveInlining)]
	public static bool GetPointer<T, TS>(TS s, out T* pointer, out long length, [CallerArgumentExpression("s")] string? sName = null) where T : unmanaged, IBaseNumber<T> where TS : class, IStorage<T, TS>
	{
		pointer = default; length = 0;
		if (s is null || !s.IsValid())
			throw new ArgumentNullException(sName);
		if (s is PureStorage<T, CudaMemoryPointer<GpuId0>>)
		{
			if (Runtime.CurrentDeviceID != 0)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId1>>)
		{
			if (Runtime.CurrentDeviceID != 1)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId2>>)
		{
			if (Runtime.CurrentDeviceID != 2)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId3>>)
		{
			if (Runtime.CurrentDeviceID != 3)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId4>>)
		{
			if (Runtime.CurrentDeviceID != 4)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId5>>)
		{
			if (Runtime.CurrentDeviceID != 5)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId6>>)
		{
			if (Runtime.CurrentDeviceID != 6)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId7>>)
		{
			if (Runtime.CurrentDeviceID != 7)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId8>>)
		{
			if (Runtime.CurrentDeviceID != 8)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId9>>)
		{
			if (Runtime.CurrentDeviceID != 9)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId10>>)
		{
			if (Runtime.CurrentDeviceID != 10)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId11>>)
		{
			if (Runtime.CurrentDeviceID != 11)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId12>>)
		{
			if (Runtime.CurrentDeviceID != 12)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId13>>)
		{
			if (Runtime.CurrentDeviceID != 13)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId14>>)
		{
			if (Runtime.CurrentDeviceID != 14)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId15>>)
		{
			if (Runtime.CurrentDeviceID != 15)
				return false;
			goto END;
		}
		return false;
	END:
		return GetPointerInner(s, out pointer, out length, sName);
	}

	[MethodImpl(MethodImplOptions.AggressiveInlining)]
	public static bool GetPointer<T, TS>(IBindedDevice api, TS s, long stride, out T* pointer, out int length, out int inc, [CallerArgumentExpression("s")] string? sName = null, [CallerArgumentExpression("stride")] string? strideName = null) where T : unmanaged, IBaseNumber<T> where TS : class, IStorage<T, TS>
	{
		pointer = default; length = 0; inc = 0;
		if (api.BindedDeviceID != Runtime.CurrentDeviceID)
			return false;
		if (s is null || !s.IsValid())
			throw new ArgumentNullException(sName);
		if (s is PureStorage<T, CudaMemoryPointer<GpuId0>>)
		{
			if (Runtime.CurrentDeviceID != 0)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId1>>)
		{
			if (Runtime.CurrentDeviceID != 1)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId2>>)
		{
			if (Runtime.CurrentDeviceID != 2)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId3>>)
		{
			if (Runtime.CurrentDeviceID != 3)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId4>>)
		{
			if (Runtime.CurrentDeviceID != 4)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId5>>)
		{
			if (Runtime.CurrentDeviceID != 5)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId6>>)
		{
			if (Runtime.CurrentDeviceID != 6)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId7>>)
		{
			if (Runtime.CurrentDeviceID != 7)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId8>>)
		{
			if (Runtime.CurrentDeviceID != 8)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId9>>)
		{
			if (Runtime.CurrentDeviceID != 9)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId10>>)
		{
			if (Runtime.CurrentDeviceID != 10)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId11>>)
		{
			if (Runtime.CurrentDeviceID != 11)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId12>>)
		{
			if (Runtime.CurrentDeviceID != 12)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId13>>)
		{
			if (Runtime.CurrentDeviceID != 13)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId14>>)
		{
			if (Runtime.CurrentDeviceID != 14)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId15>>)
		{
			if (Runtime.CurrentDeviceID != 15)
				return false;
			goto END;
		}
		return false;
	END:
		return GetPointerInner(s, stride, out pointer, out length, out inc, sName, strideName);
	}
	
	[MethodImpl(MethodImplOptions.AggressiveInlining)]
	public static bool GetPointer<T, TS>(IBindedDevice api, TS s, long stride, out T* pointer, out long length, [CallerArgumentExpression("s")] string? sName = null, [CallerArgumentExpression("stride")] string? strideName = null) where T : unmanaged, IBaseNumber<T> where TS : class, IStorage<T, TS>
	{
		pointer = default; length = 0;
		if (api.BindedDeviceID != Runtime.CurrentDeviceID)
			return false;
		if (s is null || !s.IsValid())
			throw new ArgumentNullException(sName);
		if (s is PureStorage<T, CudaMemoryPointer<GpuId0>>)
		{
			if (Runtime.CurrentDeviceID != 0)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId1>>)
		{
			if (Runtime.CurrentDeviceID != 1)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId2>>)
		{
			if (Runtime.CurrentDeviceID != 2)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId3>>)
		{
			if (Runtime.CurrentDeviceID != 3)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId4>>)
		{
			if (Runtime.CurrentDeviceID != 4)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId5>>)
		{
			if (Runtime.CurrentDeviceID != 5)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId6>>)
		{
			if (Runtime.CurrentDeviceID != 6)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId7>>)
		{
			if (Runtime.CurrentDeviceID != 7)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId8>>)
		{
			if (Runtime.CurrentDeviceID != 8)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId9>>)
		{
			if (Runtime.CurrentDeviceID != 9)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId10>>)
		{
			if (Runtime.CurrentDeviceID != 10)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId11>>)
		{
			if (Runtime.CurrentDeviceID != 11)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId12>>)
		{
			if (Runtime.CurrentDeviceID != 12)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId13>>)
		{
			if (Runtime.CurrentDeviceID != 13)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId14>>)
		{
			if (Runtime.CurrentDeviceID != 14)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId15>>)
		{
			if (Runtime.CurrentDeviceID != 15)
				return false;
			goto END;
		}
		return false;
	END:
		return GetPointerInnerLong(s, stride, out pointer, out length, sName, strideName);
	}

	[MethodImpl(MethodImplOptions.AggressiveInlining)]
	public static unsafe bool GetPointer<T, TS>(IBindedDevice api, TS? s, long m, long n, long ld, out T* pointer, out int mm, out int nn, out int lld, [CallerArgumentExpression("s")] string? sName = null, [CallerArgumentExpression("m")] string? mName = null, [CallerArgumentExpression("n")] string? nName = null, [CallerArgumentExpression("ld")] string? ldName = null) where T : unmanaged, IBaseNumber<T> where TS : class, IStorage<T, TS>
	{
		pointer = default; mm = (int)m; nn = (int)n; lld = (int)ld;
		if (api.BindedDeviceID != Runtime.CurrentDeviceID)
			return false;
		if (s is null || !s.IsValid())
			return true;
		if (s is PureStorage<T, CudaMemoryPointer<GpuId0>>)
		{
			if (Runtime.CurrentDeviceID != 0)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId1>>)
		{
			if (Runtime.CurrentDeviceID != 1)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId2>>)
		{
			if (Runtime.CurrentDeviceID != 2)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId3>>)
		{
			if (Runtime.CurrentDeviceID != 3)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId4>>)
		{
			if (Runtime.CurrentDeviceID != 4)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId5>>)
		{
			if (Runtime.CurrentDeviceID != 5)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId6>>)
		{
			if (Runtime.CurrentDeviceID != 6)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId7>>)
		{
			if (Runtime.CurrentDeviceID != 7)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId8>>)
		{
			if (Runtime.CurrentDeviceID != 8)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId9>>)
		{
			if (Runtime.CurrentDeviceID != 9)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId10>>)
		{
			if (Runtime.CurrentDeviceID != 10)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId11>>)
		{
			if (Runtime.CurrentDeviceID != 11)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId12>>)
		{
			if (Runtime.CurrentDeviceID != 12)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId13>>)
		{
			if (Runtime.CurrentDeviceID != 13)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId14>>)
		{
			if (Runtime.CurrentDeviceID != 14)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId15>>)
		{
			if (Runtime.CurrentDeviceID != 15)
				return false;
			goto END;
		}
	return false;
END:
	return GetPointerInner(s, m, n, ld, out pointer, sName, mName, nName, ldName);
	}

	[MethodImpl(MethodImplOptions.AggressiveInlining)]
	public static unsafe bool GetPointer<T, TS>(IBindedDevice api, TS? s, MatrixOperation op, long m, long n, long ld, out T* pointer, out int mm, out int nn, out int lld, [CallerArgumentExpression("s")] string? sName = null, [CallerArgumentExpression("m")] string? mName = null, [CallerArgumentExpression("n")] string? nName = null, [CallerArgumentExpression("ld")] string? ldName = null) where T : unmanaged, IBaseNumber<T> where TS : class, IStorage<T, TS>
	{
		pointer = default; mm = (int)m; nn = (int)n; lld = (int)ld;
		if (api.BindedDeviceID != Runtime.CurrentDeviceID)
			return false;
		if (s is null || !s.IsValid())
			return true;
		if (!op.CanInPlace())
		{
			(m, n) = (n, m);
			(mm, nn) = (nn, mm);
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId0>>)
		{
			if (Runtime.CurrentDeviceID != 0)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId1>>)
		{
			if (Runtime.CurrentDeviceID != 1)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId2>>)
		{
			if (Runtime.CurrentDeviceID != 2)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId3>>)
		{
			if (Runtime.CurrentDeviceID != 3)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId4>>)
		{
			if (Runtime.CurrentDeviceID != 4)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId5>>)
		{
			if (Runtime.CurrentDeviceID != 5)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId6>>)
		{
			if (Runtime.CurrentDeviceID != 6)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId7>>)
		{
			if (Runtime.CurrentDeviceID != 7)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId8>>)
		{
			if (Runtime.CurrentDeviceID != 8)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId9>>)
		{
			if (Runtime.CurrentDeviceID != 9)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId10>>)
		{
			if (Runtime.CurrentDeviceID != 10)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId11>>)
		{
			if (Runtime.CurrentDeviceID != 11)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId12>>)
		{
			if (Runtime.CurrentDeviceID != 12)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId13>>)
		{
			if (Runtime.CurrentDeviceID != 13)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId14>>)
		{
			if (Runtime.CurrentDeviceID != 14)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId15>>)
		{
			if (Runtime.CurrentDeviceID != 15)
				return false;
			goto END;
		}
	return false;
END:
	return GetPointerInner(s, m, n, ld, out pointer, sName, mName, nName, ldName);
	}
	
	[MethodImpl(MethodImplOptions.AggressiveInlining)]
	public static unsafe bool GetPointer<T, TS>(IBindedDevice api, TS? s, long m, long n, long ld, out T* pointer, [CallerArgumentExpression("s")] string? sName = null, [CallerArgumentExpression("m")] string? mName = null, [CallerArgumentExpression("n")] string? nName = null, [CallerArgumentExpression("ld")] string? ldName = null) where T : unmanaged, IBaseNumber<T> where TS : class, IStorage<T, TS>
	{
		pointer = default; mm = (int)m; nn = (int)n; lld = (int)ld;
		if (api.BindedDeviceID != Runtime.CurrentDeviceID)
			return false;
		if (s is null || !s.IsValid())
			return true;
		if (s is PureStorage<T, CudaMemoryPointer<GpuId0>>)
		{
			if (Runtime.CurrentDeviceID != 0)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId1>>)
		{
			if (Runtime.CurrentDeviceID != 1)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId2>>)
		{
			if (Runtime.CurrentDeviceID != 2)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId3>>)
		{
			if (Runtime.CurrentDeviceID != 3)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId4>>)
		{
			if (Runtime.CurrentDeviceID != 4)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId5>>)
		{
			if (Runtime.CurrentDeviceID != 5)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId6>>)
		{
			if (Runtime.CurrentDeviceID != 6)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId7>>)
		{
			if (Runtime.CurrentDeviceID != 7)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId8>>)
		{
			if (Runtime.CurrentDeviceID != 8)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId9>>)
		{
			if (Runtime.CurrentDeviceID != 9)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId10>>)
		{
			if (Runtime.CurrentDeviceID != 10)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId11>>)
		{
			if (Runtime.CurrentDeviceID != 11)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId12>>)
		{
			if (Runtime.CurrentDeviceID != 12)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId13>>)
		{
			if (Runtime.CurrentDeviceID != 13)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId14>>)
		{
			if (Runtime.CurrentDeviceID != 14)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId15>>)
		{
			if (Runtime.CurrentDeviceID != 15)
				return false;
			goto END;
		}
	return false;
END:
	return GetPointerLongInner(s, m, n, ld, out pointer, sName, mName, nName, ldName);
	}

	[MethodImpl(MethodImplOptions.AggressiveInlining)]
	public static unsafe bool GetPointer<T, TInd, TS, TSInd>(IBindedDevice api, TS s, TSInd sInd, out T* pointer, out TInd* pointerInd, out long length, [CallerArgumentExpression("s")] string? sName = null, [CallerArgumentExpression("sInd")] string? sIndName = null) where T : unmanaged, IBaseNumber<T> where TS : class, IStorage<T, TS> where TInd : unmanaged, IBinaryInt<TInd> where TSInd : class, IStorage<TInd, TSInd>
	{
		pointer = default; pointerInd = default; length = 0;
		if (api.BindedDeviceID != Runtime.CurrentDeviceID)
			return false;
		if (!TInd.Type.IsInteger() || (sizeof(TInd) != sizeof(int) && sizeof(TInd) != sizeof(long)))
			return false;
		if (s is null || !s.IsValid())
			throw new ArgumentNullException(sName);
		if (sInd is null || !sInd.IsValid())
			throw new ArgumentNullException(sIndName);
		if (s is PureStorage<T, CudaMemoryPointer<GpuId0>> && sInd is PureStorage<TInd, CudaMemoryPointer<GpuId0>>)
		{
			if (Runtime.CurrentDeviceID != 0)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId1>> && sInd is PureStorage<TInd, CudaMemoryPointer<GpuId1>>)
		{
			if (Runtime.CurrentDeviceID != 1)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId2>> && sInd is PureStorage<TInd, CudaMemoryPointer<GpuId2>>)
		{
			if (Runtime.CurrentDeviceID != 2)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId3>> && sInd is PureStorage<TInd, CudaMemoryPointer<GpuId3>>)
		{
			if (Runtime.CurrentDeviceID != 3)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId4>> && sInd is PureStorage<TInd, CudaMemoryPointer<GpuId4>>)
		{
			if (Runtime.CurrentDeviceID != 4)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId5>> && sInd is PureStorage<TInd, CudaMemoryPointer<GpuId5>>)
		{
			if (Runtime.CurrentDeviceID != 5)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId6>> && sInd is PureStorage<TInd, CudaMemoryPointer<GpuId6>>)
		{
			if (Runtime.CurrentDeviceID != 6)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId7>> && sInd is PureStorage<TInd, CudaMemoryPointer<GpuId7>>)
		{
			if (Runtime.CurrentDeviceID != 7)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId8>> && sInd is PureStorage<TInd, CudaMemoryPointer<GpuId8>>)
		{
			if (Runtime.CurrentDeviceID != 8)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId9>> && sInd is PureStorage<TInd, CudaMemoryPointer<GpuId9>>)
		{
			if (Runtime.CurrentDeviceID != 9)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId10>> && sInd is PureStorage<TInd, CudaMemoryPointer<GpuId10>>)
		{
			if (Runtime.CurrentDeviceID != 10)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId11>> && sInd is PureStorage<TInd, CudaMemoryPointer<GpuId11>>)
		{
			if (Runtime.CurrentDeviceID != 11)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId12>> && sInd is PureStorage<TInd, CudaMemoryPointer<GpuId12>>)
		{
			if (Runtime.CurrentDeviceID != 12)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId13>> && sInd is PureStorage<TInd, CudaMemoryPointer<GpuId13>>)
		{
			if (Runtime.CurrentDeviceID != 13)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId14>> && sInd is PureStorage<TInd, CudaMemoryPointer<GpuId14>>)
		{
			if (Runtime.CurrentDeviceID != 14)
				return false;
			goto END;
		}
		if (s is PureStorage<T, CudaMemoryPointer<GpuId15>> && sInd is PureStorage<TInd, CudaMemoryPointer<GpuId15>>)
		{
			if (Runtime.CurrentDeviceID != 15)
				return false;
			goto END;
		}
		return GetPointerInner(s, sInd, out pointer, out pointerInd, sName, sIndName);
	}

	[MethodImpl(MethodImplOptions.AggressiveInlining)]
	private static bool GetPointer<T, TInd, TS, TSInd>(TS val, TSInd rowInd, TSInd colInd) where T : unmanaged, IBaseNumber<T> where TS : class, IStorage<T, TS> where TInd : unmanaged, IBinaryInt<TInd> where TSInd : class, IStorage<TInd, TSInd>
	{
		if (val is PureStorage<T, CudaMemoryPointer<GpuId0>> && rowInd is PureStorage<TInd, CudaMemoryPointer<GpuId0>> && colInd is PureStorage<TInd, CudaMemoryPointer<GpuId0>>)
		{
			if (Runtime.CurrentDeviceID != 0)
				return false;
			goto END;
		}
		if (val is PureStorage<T, CudaMemoryPointer<GpuId1>> && rowInd is PureStorage<TInd, CudaMemoryPointer<GpuId1>> && colInd is PureStorage<TInd, CudaMemoryPointer<GpuId1>>)
		{
			if (Runtime.CurrentDeviceID != 1)
				return false;
			goto END;
		}
		if (val is PureStorage<T, CudaMemoryPointer<GpuId2>> && rowInd is PureStorage<TInd, CudaMemoryPointer<GpuId2>> && colInd is PureStorage<TInd, CudaMemoryPointer<GpuId2>>)
		{
			if (Runtime.CurrentDeviceID != 2)
				return false;
			goto END;
		}
		if (val is PureStorage<T, CudaMemoryPointer<GpuId3>> && rowInd is PureStorage<TInd, CudaMemoryPointer<GpuId3>> && colInd is PureStorage<TInd, CudaMemoryPointer<GpuId3>>)
		{
			if (Runtime.CurrentDeviceID != 3)
				return false;
			goto END;
		}
		if (val is PureStorage<T, CudaMemoryPointer<GpuId4>> && rowInd is PureStorage<TInd, CudaMemoryPointer<GpuId4>> && colInd is PureStorage<TInd, CudaMemoryPointer<GpuId4>>)
		{
			if (Runtime.CurrentDeviceID != 4)
				return false;
			goto END;
		}
		if (val is PureStorage<T, CudaMemoryPointer<GpuId5>> && rowInd is PureStorage<TInd, CudaMemoryPointer<GpuId5>> && colInd is PureStorage<TInd, CudaMemoryPointer<GpuId5>>)
		{
			if (Runtime.CurrentDeviceID != 5)
				return false;
			goto END;
		}
		if (val is PureStorage<T, CudaMemoryPointer<GpuId6>> && rowInd is PureStorage<TInd, CudaMemoryPointer<GpuId6>> && colInd is PureStorage<TInd, CudaMemoryPointer<GpuId6>>)
		{
			if (Runtime.CurrentDeviceID != 6)
				return false;
			goto END;
		}
		if (val is PureStorage<T, CudaMemoryPointer<GpuId7>> && rowInd is PureStorage<TInd, CudaMemoryPointer<GpuId7>> && colInd is PureStorage<TInd, CudaMemoryPointer<GpuId7>>)
		{
			if (Runtime.CurrentDeviceID != 7)
				return false;
			goto END;
		}
		if (val is PureStorage<T, CudaMemoryPointer<GpuId8>> && rowInd is PureStorage<TInd, CudaMemoryPointer<GpuId8>> && colInd is PureStorage<TInd, CudaMemoryPointer<GpuId8>>)
		{
			if (Runtime.CurrentDeviceID != 8)
				return false;
			goto END;
		}
		if (val is PureStorage<T, CudaMemoryPointer<GpuId9>> && rowInd is PureStorage<TInd, CudaMemoryPointer<GpuId9>> && colInd is PureStorage<TInd, CudaMemoryPointer<GpuId9>>)
		{
			if (Runtime.CurrentDeviceID != 9)
				return false;
			goto END;
		}
		if (val is PureStorage<T, CudaMemoryPointer<GpuId10>> && rowInd is PureStorage<TInd, CudaMemoryPointer<GpuId10>> && colInd is PureStorage<TInd, CudaMemoryPointer<GpuId10>>)
		{
			if (Runtime.CurrentDeviceID != 10)
				return false;
			goto END;
		}
		if (val is PureStorage<T, CudaMemoryPointer<GpuId11>> && rowInd is PureStorage<TInd, CudaMemoryPointer<GpuId11>> && colInd is PureStorage<TInd, CudaMemoryPointer<GpuId11>>)
		{
			if (Runtime.CurrentDeviceID != 11)
				return false;
			goto END;
		}
		if (val is PureStorage<T, CudaMemoryPointer<GpuId12>> && rowInd is PureStorage<TInd, CudaMemoryPointer<GpuId12>> && colInd is PureStorage<TInd, CudaMemoryPointer<GpuId12>>)
		{
			if (Runtime.CurrentDeviceID != 12)
				return false;
			goto END;
		}
		if (val is PureStorage<T, CudaMemoryPointer<GpuId13>> && rowInd is PureStorage<TInd, CudaMemoryPointer<GpuId13>> && colInd is PureStorage<TInd, CudaMemoryPointer<GpuId13>>)
		{
			if (Runtime.CurrentDeviceID != 13)
				return false;
			goto END;
		}
		if (val is PureStorage<T, CudaMemoryPointer<GpuId14>> && rowInd is PureStorage<TInd, CudaMemoryPointer<GpuId14>> && colInd is PureStorage<TInd, CudaMemoryPointer<GpuId14>>)
		{
			if (Runtime.CurrentDeviceID != 14)
				return false;
			goto END;
		}
		if (val is PureStorage<T, CudaMemoryPointer<GpuId15>> && rowInd is PureStorage<TInd, CudaMemoryPointer<GpuId15>> && colInd is PureStorage<TInd, CudaMemoryPointer<GpuId15>>)
		{
			if (Runtime.CurrentDeviceID != 15)
				return false;
			goto END;
		}
		return true;
	}
	#endregion
}